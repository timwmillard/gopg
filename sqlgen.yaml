version: 1

connection:
  url: postgresql://root:postgres@localhost:55432/gopg
  env_var: DATABASE_URL 

packages:
  - name: "model"
    path: "examples/model"
    queries: "examples/queries"
    gen: "pgx" 

typemap:

